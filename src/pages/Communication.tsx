import { Radio } from 'lucide-react';
import PlaceholderPage from './PlaceholderPage';
export default function Communication() {
  return <PlaceholderPage icon={<Radio size={56} />} title="Communication" description="Detailed communication network management. Configure rescue node mesh, view signal maps, manage frequency bands, and monitor link quality for all connected communication nodes." phase="Requires communication hardware integration" />;
}
